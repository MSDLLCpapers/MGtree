nextflow_process {

    name "Test Process PARSESAM"
    script "modules/local/parsesam/main.nf"
    process "PARSESAM"

    test("parseSAM.py") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = Channel.of(
                    [
                        [
                            id : 'test'
                        ],
                        [
                            file("${projectDir}/test-data/testSRR11028620_mapped.sam")
                        ]
                    ]
                )
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() },
            )
        }

    }

}
